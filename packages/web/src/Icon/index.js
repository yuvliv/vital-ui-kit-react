import { Props as IconProps } from './Icon';

export { default } from './Icon';
export { default as withIcon } from './components/withIcon';
export { default as IconProvider } from './components/IconProvider';

export {
    IconProps
}